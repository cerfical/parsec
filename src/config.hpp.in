#pragma once

#define PARSEC_VERSION_MAJOR ${PROJECT_VERSION_MAJOR}
#define PARSEC_VERSION_MINOR ${PROJECT_VERSION_MINOR}
#define PARSEC_VERSION_PATCH ${PROJECT_VERSION_PATCH}

#define PARSEC_VERSION "${PROJECT_VERSION}"
#define PARSEC_DESCRIPTION "${PROJECT_DESCRIPTION}"
